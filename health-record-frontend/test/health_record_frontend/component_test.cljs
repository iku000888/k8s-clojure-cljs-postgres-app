(ns health-record-frontend.component-test
  (:require [cljs.test :as t]
            ["@testing-library/react" :as rt]
            [helix.core :refer [$]]
            [helix.dom :as d]
            #_[health-record-frontend.page :as page]))

#_(t/deftest foo-test
    (t/is (= 1 1))
    #_(rt/render ($ page/app "foo"))
    )
